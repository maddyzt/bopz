import React from 'react';
interface UserObject {
    username: string;
    avatar_url: string;
    name: string;
}
interface Props {
    userList: Array<UserObject>;
    searchTerm: string;
    wrapperRef: React.RefObject<any>;
    cancelSearch: () => void;
    selectUser: (v: string) => void;
}
declare const UserWidget: ({ userList, searchTerm, wrapperRef, cancelSearch, selectUser }: Props) => JSX.Element | null;
export default UserWidget;
